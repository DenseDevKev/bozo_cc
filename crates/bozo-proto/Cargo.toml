[package]
name = "bozo-proto"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "BMAP protocol codec and IPC types for Bose QC Ultra headphones over BLE"
readme = "README.md"
keywords = ["bose", "bluetooth", "ble", "headphones", "bmap"]
categories = ["encoding", "hardware-support", "parser-implementations"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["io-util"] }
num_enum = "0.7"
